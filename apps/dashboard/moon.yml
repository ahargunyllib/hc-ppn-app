id: dashboard
language: typescript

tasks:
  dev:
    command: "pnpm dev"
    options:
      runInCI: false

  build:
    command: "pnpm build"
    description: "Build dashboard app"
    options:
      runInCI: false

  sync:
    command: "pnpm install"
    description: "Sync project dependencies"
    options:
      runInCI: false
